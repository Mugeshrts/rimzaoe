import 'package:flutter/material.dart';
import 'package:rimza1/data/user.dart';

class ModeSelectionScreen extends StatefulWidget {
  const ModeSelectionScreen({super.key, required DeviceModel device});

  @override
  State<ModeSelectionScreen> createState() => _MyWidgetState();
}

class _MyWidgetState extends State<ModeSelectionScreen> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}